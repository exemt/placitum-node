# Политика поверх облегчённого набора: та же, что у default, -- DoS считает
# локальный слой модуля, а не CRS.
SecRuleRemoveByTag "attack-dos"

# Маркер профиля: в audit видно, что применился light, а не default.
SecAction "id:1000000,phase:1,nolog,pass,t:none,setvar:tx.modsec_profile=light"
