# store без снимка объекта: поднимать нечего, такой опции нет.
#
# Проверка обязана ЗАВЕРШИТЬСЯ НЕУДАЧЕЙ:
#
#     nginx: [emerg] waf: waf_send request body=store, but the object is not
#     captured on this route

load_module modules/ngx_http_waf_module.so;

events { }

http {
    waf_node_id       unit;
    waf_agent_socket /tmp/waf.sock;
    waf_bus nats://127.0.0.1:4222;
    waf_store driver=redis url=redis://127.0.0.1:6379 ttl=30s retain_ttl=5m;

    waf_inspector mask subject=waf.req.mask;

    server {
        listen 8080;
        waf on;

        location /api/ {
            waf_capture request headers args;
            waf_inspect request mask wave=0 timeout=20ms;
            waf_send    request body=store;
            return 204;
        }
    }
}
