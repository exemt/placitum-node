# keep= на фазе ответа: держать состояние просят ту фазу, которая его выдаёт.
# Потребляющая фаза говорит resume=.
#
# Проверка обязана ЗАВЕРШИТЬСЯ НЕУДАЧЕЙ:
#
#     nginx: [emerg] waf: keep= belongs to the request phase, which hands the
#            state over; response consumes it with resume=

load_module modules/ngx_http_waf_module.so;

events { }

http {
    waf_node_id       unit;
    waf_agent_socket /tmp/waf.sock;
    waf_bus nats://127.0.0.1:4222;
    waf_store driver=redis url=redis://127.0.0.1:6379 ttl=30s retain_ttl=5m;

    waf_inspector modsec subject=waf.req.modsec;

    server {
        listen 8080;
        waf on;

        waf_capture request headers=8k args=8k;

        location /leaks/ {
            waf_capture  response headers=8k body=64k;

            waf_inspect  request  modsec wave=0 timeout=800ms;
            waf_inspect  response modsec wave=0 timeout=800ms keep=on;

            return 204;
        }
    }
}
