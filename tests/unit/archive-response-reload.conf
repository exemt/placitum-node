# reload шире снимка вне фазы запроса: оригинал брать неоткуда -- всё, что за
# снимком, уже ушло клиенту, а потокового размещения обменник не умеет.
#
# Проверка обязана ЗАВЕРШИТЬСЯ НЕУДАЧЕЙ:
#
#     nginx: [emerg] waf: waf_archive reload body is wider than capture, and
#                    outside the request phase there is nothing to reload from

load_module modules/ngx_http_waf_module.so;

events { }

http {
    waf_node_id       unit;
    waf_agent_socket /tmp/waf.sock;
    waf_bus nats://127.0.0.1:4222;
    waf_store driver=redis url=redis://127.0.0.1:6379 ttl=30s retain_ttl=5m;

    waf_inspector ip  subject=waf.req.ip;
    waf_inspector dlp subject=waf.rsp.dlp;

    server {
        listen 8080;
        waf on;

        waf_capture request headers=8k args=8k;
        waf_inspect request ip wave=0 timeout=5ms;

        location /api/ {
            waf_capture  response body=8k;
            waf_archive  response body;
            waf_archive  response reload body=64k;

            waf_deadline response 200ms;
            waf_exception response timeout deny;
            waf_inspect  response dlp wave=0 timeout=100ms;

            return 204;
        }
    }
}
