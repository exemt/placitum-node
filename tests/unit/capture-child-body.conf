# Своя строка перекрывает только названный объект: body=4k не сбрасывает
# headers/args родителя. Иначе archive headers без reload шире capture.
#
# Проверка обязана ЗАВЕРШИТЬСЯ УСПЕХОМ:
#
#     docker run --rm -v "$PWD/nginx/tests/unit:/t:ro" waf-nginx \
#         nginx -t -c /t/capture-child-body.conf

load_module modules/ngx_http_waf_module.so;

events { }

http {
    waf_node_id       unit;
    waf_agent_socket /tmp/waf.sock;
    waf_bus nats://127.0.0.1:4222;
    waf_deny_response blocked status=403;
    waf_store driver=redis url=redis://127.0.0.1:6379 ttl=30s retain_ttl=5m;

    waf_inspector ip subject=waf.req.ip;

    waf_capture request headers=64k args=64k;
    waf_archive request headers args when=deny;

    server {
        listen 8080;

        waf on;
        waf_inspect request ip wave=0;

        location /api/ {
            waf_capture request body=4k;
            return 204;
        }
    }
}
