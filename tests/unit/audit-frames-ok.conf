# Политика аудита кадров: off | deny (умолчание) | all [sample=<n>].
# sample= только у all.
#
# Проверка обязана ЗАВЕРШИТЬСЯ УСПЕХОМ:
#
#     docker run --rm -v "$PWD/nginx/tests/unit:/t:ro" waf-nginx \
#         nginx -t -c /t/audit-frames-ok.conf

load_module modules/ngx_http_waf_module.so;

events { }

http {
    waf_node_id       unit;
    waf_agent_socket /tmp/waf.sock;
    waf_bus nats://127.0.0.1:4222;
    waf_store driver=redis url=redis://127.0.0.1:6379 ttl=30s;

    waf_inspector ws_rules subject=waf.frm.rules;

    server {
        listen 8080;
        waf on;

        waf_audit_frames deny;

        location /ws/ {
            waf_audit_frames all sample=10;
            waf_inspect frame ws_rules wave=0 timeout=5ms;
            waf_capture frame body=16k;
            return 204;
        }

        location /quiet/ {
            waf_audit_frames off;
            return 204;
        }
    }
}
