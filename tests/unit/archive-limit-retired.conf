# Общий limit= снят: размер живёт на объекте (body=8k), иначе одна строка
# с заголовками и телом не могла бы резать только тело.
#
# Проверка обязана ЗАВЕРШИТЬСЯ НЕУДАЧЕЙ:
#
#     docker run --rm -v "$PWD/nginx/tests/unit:/t:ro" waf-nginx \
#         nginx -t -c /t/archive-limit-retired.conf
#
#     nginx: [emerg] waf_archive: limit= is gone; write the size on the object
#            (body=8k), or omit it to archive the whole object

load_module modules/ngx_http_waf_module.so;

events { }

http {
    waf_node_id unit;

    waf_bus nats://127.0.0.1:4222;
    waf_agent_socket /tmp/waf.sock;
    waf_deny_response blocked status=403;

    waf_store driver=redis url=redis://127.0.0.1:6379 ttl=30s retain_ttl=5m;

    waf_inspector ip subject=waf.req.ip;
    server {
        listen 8080;

        waf on;
        waf_inspect request ip wave=0;
        waf_capture request headers args body;

        location / {
            waf_archive request body ttl=180d when=deny limit=8k;
            return 204;
        }
    }
}
