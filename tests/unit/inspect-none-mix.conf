# none нельзя мешать с именами той же фазы.
#
# Проверка обязана ЗАВЕРШИТЬСЯ НЕУДАЧЕЙ:
#
#     nginx: [emerg] waf: waf_inspect request none cannot mix with named
#            inspectors of the same phase

load_module modules/ngx_http_waf_module.so;

events { }

http {
    waf_node_id       unit;
    waf_agent_socket /tmp/waf.sock;
    waf_bus nats://127.0.0.1:4222;
    waf_deny_response blocked status=403;

    waf_inspector ip subject=waf.req.ip;

    server {
        listen 8080;
        waf on;
        waf_inspect request none;
        waf_inspect request ip wave=0;

        location / {
            return 204;
        }
    }
}
